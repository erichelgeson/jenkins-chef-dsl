/usr/bin/java -jar slave.jar -jnlpUrl http://<%= node['jenkins']['master']['user'] %>/computer/<%= node[:hostname] %>/slave-agent.jnlp -secret <%= `curl -d "script=for (aSlave in hudson.model.Hudson.instance.slaves){ if (aSlave.name == '#{node[:hostname]}') { println aSlave.getComputer().getJnlpMac() } }" -X POST http://<%= node['jenkins']['master']['user'] %>:<%= node['jenkins']['master']['password'] %><%= node['jenkins']['master']['user'] %>/scriptText`.chomp %>
